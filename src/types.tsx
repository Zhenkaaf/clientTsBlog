export interface IUserInfo {
    _id: string;
    email: string;
    posts: [];
    createdAt: string;
    updatedAt: string;
}

export interface IFormInputs {
    email: string;
    password: string;
}
